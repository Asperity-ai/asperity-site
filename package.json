{
  "name": "asperity-site",
  "version": "0.1.0",
  "private": true,
  "description": "Asperity Industries marketing site — plain HTML + Tailwind v4.",
  "scripts": {
    "dev": "tailwindcss -i ./input.css -o ./dist/style.css --watch",
    "build": "tailwindcss -i ./input.css -o ./dist/style.css --minify"
  },
  "devDependencies": {
    "@tailwindcss/cli": "^4.0.0"
  }
}
